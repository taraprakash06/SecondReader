services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: secondreader
      POSTGRES_PASSWORD: secondreader
      POSTGRES_DB: secondreader
    ports:
      - "5432:5432"
    volumes:
      - secondreader_pgdata:/var/lib/postgresql/data

volumes:
  secondreader_pgdata:
